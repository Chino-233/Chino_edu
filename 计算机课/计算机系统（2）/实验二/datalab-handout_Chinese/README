***********************
CS:APP 数据实验
给学生的指示
***********************

你的目标是修改你的 bits.c 副本，使其通过所有 btest 测试，同时不违反任何编码准则。


*********
0. 文件:
*********

Makefile	- 生成 btest、fshow 和 ishow
README		- 本文件
bits.c		- 你将修改并提交的文件
bits.h		- 头文件
btest.c		- 主要的 btest 程序
  btest.h	- 用于构建 btest
  decl.c	- 用于构建 btest
  tests.c       - 用于构建 btest
  tests-header.c- 用于构建 btest
dlc*		- 规则检查编译器二进制文件（数据实验编译器）
driver.pl*	- 驱动程序，使用 btest 和 dlc 自动评分 bits.c
Driverhdrs.pm   - 可选"打败教授"比赛的头文件
fshow.c		- 用于检查浮点表示的实用程序
ishow.c		- 用于检查整数表示的实用程序

***********************************************************
1. 修改 bits.c 并检查其是否符合 dlc 要求
***********************************************************

重要：在开始之前，请仔细阅读 bits.c 文件中的指示。这些给出了编码规则，如果你想获得全分，你需要遵循这些规则。

使用 dlc 编译器（./dlc）自动检查你的 bits.c 版本是否符合编码准则：

       unix> ./dlc bits.c

如果你的代码没有问题，dlc 将静默返回。
否则它会打印出标记任何问题的消息。使用 -e 开关运行 dlc：

        unix> ./dlc -e bits.c  

将使 dlc 打印每个函数使用的运算符数量。

一旦你有了合法的解决方案，你可以使用 ./btest 程序测试其正确性。

*********************
2. 使用 btest 测试
*********************

此目录中的 Makefile 将你的 bits.c 版本与其他代码一起编译，创建一个程序（或测试工具）名为 btest。

要编译和运行 btest 程序，请输入：

    unix> make btest
    unix> ./btest [可选命令行参数]

每次更改 bits.c 程序时，你都需要重新编译 btest。
当从一个平台移动到另一个平台时，你会想要
删除旧版本的 btest 并生成一个新版本。使用以下命令：

    unix> make clean
    unix> make btest

Btest 通过对每个函数运行数百万个测试用例来测试你的代码的正确性。
它测试围绕众所周知的边缘情况如 Tmin 和零的整数谜题的宽范围，
以及围绕零、inf 和非规范化和规范化数之间边界的浮点谜题。
当 btest 在你的某个函数中检测到错误时，它会打印出失败的测试、
不正确的结果和预期结果，然后终止该函数的测试。

以下是 btest 的命令行选项：

  unix> ./btest -h
  用法: ./btest [-hg] [-r <n>] [-f <name> [-1|-2|-3 <val>]*] [-T <time limit>]
    -1 <val>  指定第一函数参数
    -2 <val>  指定第二函数参数
    -3 <val>  指定第三函数参数
    -f <name> 只测试指定的函数
    -g        格式化输出用于自动评分，不显示错误消息
    -h        打印此消息
    -r <n>    给所有问题统一权重 n
    -T <lim>  设置超时限制为 lim

示例：

  测试所有函数的正确性并打印错误消息：
  unix> ./btest

  以简洁形式测试所有函数，不显示错误消息：
  unix> ./btest -g

  测试函数 foo 的正确性：
  unix> ./btest -f foo

  使用特定参数测试函数 foo 的正确性：
  unix> ./btest -f foo -1 27 -2 0xf

Btest 不检查你的代码是否符合编码准则。使用 dlc 来做那个。

*******************
3. 辅助程序
*******************

我们已经包含了 ishow 和 fshow 程序，帮助你分别解读整数和浮点表示。
每个程序都接受一个十进制或十六进制数作为参数。要构建它们，输入：

    unix> make

使用示例：

    unix> ./ishow 0x27
    十六进制 = 0x00000027,	有符号 = 39,	无符号 = 39

    unix> ./ishow 27
    十六进制 = 0x0000001b,	有符号 = 27,	无符号 = 27

    unix> ./fshow 0x15213243
    浮点值 3.255334057e-26
    位表示 0x15213243, 符号 = 0, 指数 = 0x2a, 小数 = 0x213243
    规格化。 +1.2593463659 X 2^(-85)

    linux> ./fshow 15213243
    浮点值 2.131829405e-38
    位表示 0x00e822bb, 符号 = 0, 指数 = 0x01, 小数 = 0x6822bb
    规格化。 +1.8135598898 X 2^(-126)